# Databricks notebook source
# MAGIC %md
# MAGIC ### **Easy**
# MAGIC
# MAGIC **1. Simple List Comprehension**
# MAGIC * **Task:** 
# MAGIC   1. Define a list `base_list = [1, 2, 3, 4]`.
# MAGIC   2. Use a list comprehension to create a new list where every element is multiplied by 3.
# MAGIC
# MAGIC

# COMMAND ----------



# COMMAND ----------

# MAGIC %md
# MAGIC **2. Simple Dictionary Comprehension**
# MAGIC * **Task:** 
# MAGIC   1. Define a list `keys = ["a", "b", "c"]`.
# MAGIC   2. Use a dictionary comprehension to create a dict where the key is the string and the value is the string capitalized.
# MAGIC

# COMMAND ----------



# COMMAND ----------

# MAGIC %md
# MAGIC ### **Medium**
# MAGIC
# MAGIC **3. List Comprehension with If Filter**
# MAGIC * **Task:** 
# MAGIC   1. Define `scores = [45, 80, 20, 95, 100, 33]`.
# MAGIC   2. Use list comprehension to extract only the scores that are greater than 50.
# MAGIC

# COMMAND ----------



# COMMAND ----------

# MAGIC %md
# MAGIC **4. List Comprehension with If/Else Transformation**
# MAGIC * **Task:** 
# MAGIC   1. Define `numbers = [1, 2, 3, 4]`.
# MAGIC   2. Create a list comprehension that outputs `"Even"` if the number is even, and `"Odd"` if the number is odd.
# MAGIC
# MAGIC

# COMMAND ----------



# COMMAND ----------

# MAGIC %md
# MAGIC **5. Dictionary Comprehension with If Filter**
# MAGIC * **Task:** 
# MAGIC   1. Define `prices = {"apple": 10, "banana": 20, "cherry": 5}`.
# MAGIC   2. Use dict comprehension to create a new dictionary keeping only items where the price is 10 or higher.
# MAGIC

# COMMAND ----------



# COMMAND ----------

# MAGIC %md
# MAGIC ### **Hard**
# MAGIC
# MAGIC **6. List Comprehension: If/Else Transform + If Filter**
# MAGIC * **Task:** 
# MAGIC   1. Define `raw_data = [1, 2, 3, 4, 5, 6, 7, 8]`.
# MAGIC   2. Using list comprehension, multiply the number by 100 if it is exactly 4, otherwise keep it as is.
# MAGIC   3. Apply an `if` filter at the end so this is ONLY applied to even numbers.
# MAGIC

# COMMAND ----------



# COMMAND ----------

# MAGIC %md
# MAGIC **7. Dict Comprehension: If/Else Transform + If Filter**
# MAGIC * **Task:** 
# MAGIC   1. Define `values = [10, 15, 20, 25, 30]`.
# MAGIC   2. Create a dict comprehension where the key is the number, and the value is `num * 2` if the number is 20, else keep it as `num`.
# MAGIC   3. Add an `if` filter so the dictionary ONLY contains numbers divisible by 10.
# MAGIC

# COMMAND ----------

